@model  DateTime?
@{
    bool enable = true;
    if (ViewData.ModelMetadata.AdditionalValues.ContainsKey("DateEnable"))
    {
        enable = Convert.ToBoolean(ViewData.ModelMetadata.AdditionalValues["DateEnable"]);
    }
    
}

@(Html.Kendo().DatePickerFor(m => m).Enable(enable))